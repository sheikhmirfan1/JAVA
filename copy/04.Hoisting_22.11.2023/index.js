'use strict';
// console.log() = (CLG)
console.log('CLG before:', nameOfCat);
var nameOfCat = 'Sherkhan';
console.log('CLG after:', nameOfCat);

// Expression - an expression is a bit of JavaScript code that produces a value.
let currentYear = 2023; // 2023 is the expression
let dateOfBirth = 1990; // 1990 is the expression
let currentAge = currentYear - dateOfBirth; // Arithmetic expression
console.log(currentAge);
let addition = function () {}; // function expression

// Statement - a statement is an instruction for the computer to do something.
let myBugattiExists = true; //
let suspectsHeight = '185cm';
